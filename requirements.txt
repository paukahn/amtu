# requirements.txt - SOLO dependencias de runtime de la app SP-API.
#
# El fichero original era un pip freeze completo de la maquina de desarrollo:
# mezclaba el runtime con herramientas pesadas (semgrep, safety, pip-audit,
# pysonar, cyclonedx-python-lib, mcp, opentelemetry-*, uvicorn, starlette, nltk)
# cuyas versiones fijadas chocan entre si al resolver -> de ahi los conflictos al
# instalar. Esas herramientas NO se necesitan para EJECUTAR la app.
#
# Versiones fijadas a las que coexisten verificadamente en Python 3.10-3.13
# (el servidor usa 3.10.12; todas tienen wheel cp310 en Linux).

# --- Directas (lo que importa el codigo) ---
httpx==0.28.1
tenacity==9.1.2
pydantic==2.12.4
pandas==2.2.3
pycryptodome==3.23.0
paramiko==4.0.0
python-dateutil==2.9.0.post0
colorama==0.4.6

# --- Transitivas (fijadas para instalacion reproducible y sin conflictos) ---
annotated-types==0.7.0
anyio==4.10.0
bcrypt==5.0.0
certifi==2025.4.26
cffi==2.0.0
cryptography==45.0.6
h11==0.16.0
httpcore==1.0.9
idna==3.10
invoke==3.0.3
numpy==2.2.5
pycparser==2.22
pydantic_core==2.41.5
PyNaCl==1.6.2
pytz==2025.2
six==1.17.0
sniffio==1.3.1
typing_extensions==4.15.0
typing-inspection==0.4.2
tzdata==2025.2